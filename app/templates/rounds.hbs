<div class="rounds-list">
  {{#each (sort-by "createdAt:desc" model) as |round|}}
    <div class="rounds-list-item">
      <LinkTo @route="round" @model={{round.id}}>
        {{format-time round.createdAt format="long"}}
      </LinkTo>
    </div>
  {{else}}
    <p>
      {{t "rounds.none"}}
    </p>
  {{/each}}
</div>
