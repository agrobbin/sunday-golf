{{#if model}}
  <ul>
    {{#each (sort-by 'createdAt:desc' model) as |round|}}
      <li>
        {{moment-format round.createdAt 'MMMM Do, YYYY'}}
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>You have no rounds.</p>
{{/if}}
